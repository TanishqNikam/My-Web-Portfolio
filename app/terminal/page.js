import TerminalClient from "@/components/TerminalClient";

export const metadata = {
    title: "Terminal | Tanishq Nikam",
    description: "Interactive terminal — explore Tanishq Nikam's portfolio through a hacker-themed command line.",
};

export default function TerminalPage() {
    return <TerminalClient />;
}
